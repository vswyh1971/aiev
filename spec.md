# 智能大模型安全评估系统 - 问题修复规格说明

## 项目概述
修复智能大模型安全评估系统中存在的三个关键功能异常问题，确保系统稳定运行。

## 当前问题描述

### 问题1：访问页面闪烁（登录失败页面短暂显示）
**现象**：访问 http://localhost:3000/ 时，会先短暂显示登录失败页面（持续不到1秒），然后才正常显示登录页面或仪表板。

**影响范围**：所有用户首次访问系统时都会遇到此问题。

**预期行为**：
- 未登录用户：直接显示干净的登录页面
- 已登录用户：验证token成功后直接显示仪表板，无闪烁

### 问题2：点击菜单白屏且地址栏不更新
**现象**：登录后点击"数据集管理"等菜单项时，页面出现白屏，浏览器地址栏仍显示 http://localhost:3000/ 而不跳转。

**影响范围**：所有已登录用户在切换页面时遇到此问题。

**预期行为**：
- 点击侧边栏菜单项后，主内容区域应立即显示对应页面内容
- 页面应保持单页应用特性，地址栏保持不变（这是正常的SPA行为）

### 问题3：上传数据集失败
**现象**：尝试上传数据集文件时，显示"上传失败：failed to fetch"错误。

**根本原因分析**：
- 后端日志显示：`sqlite3.OperationalError: table datasets has no column named file_type`
- 数据库表结构与代码期望不一致，缺少必要字段

**预期行为**：
- 用户可以选择CSV、Excel、JSON格式的数据集文件并成功上传
- 上传后数据集应出现在数据集列表中

## 技术架构

### 前端技术栈
- **框架**：Vue 3 (CDN版本)
- **UI库**：Bootstrap 5 + Bootstrap Icons
- **HTTP客户端**：原生Fetch API
- **状态管理**：Vue Composition API (ref, reactive)
- **本地存储**：localStorage (存储token、用户信息、当前页面)

### 后端技术栈
- **框架**：FastAPI (Python)
- **数据库**：SQLite
- **认证**：JWT (python-jose库)
- **文件处理**：pandas (读取CSV/Excel/JSON)

### 关键文件
- `frontend/index.html` - 前端单页应用（包含所有HTML/CSS/JS）
- `backend/main.py` - 后端API服务
- `backend/llm_security_evaluation.db` - SQLite数据库

## 解决方案设计

### 方案1：修复页面闪烁问题
**策略**：优化初始化流程，避免状态不一致导致的视觉闪烁

**具体实现**：
1. 在onMounted中添加初始化标志位 `isInitializing`
2. 验证token之前不渲染任何页面内容（显示加载指示器）
3. 验证完成后一次性设置所有状态
4. 使用nextTick确保DOM更新完成后再显示内容

**关键代码位置**：`frontend/index.html` 第1577-1604行 (onMounted函数)

### 方案2：修复菜单白屏问题
**策略**：增强数据加载的容错性和响应式更新机制

**具体实现**：
1. 为所有load*函数添加完整的try-catch错误处理
2. 确保数据数组初始化为空数组而非undefined
3. 在模板中使用可选链操作符防止空值访问
4. 添加加载状态指示器，提升用户体验
5. 监听currentPage变化时使用async/await等待数据加载完成

**关键代码位置**：
- `frontend/index.html` 第1240-1247行 (loadDatasets函数)
- `frontend/index.html` 第1272-1282行 (watch监听器)
- `frontend/index.html` 第442-495行 (数据集页面模板)

### 方案3：修复上传数据集失败问题
**策略**：数据库表结构升级与向后兼容

**具体实现**：
1. 在数据库初始化时检查并自动添加缺失列
2. 使用ALTER TABLE语句安全地升级表结构
3. 添加try-except捕获列已存在的情况
4. 设置合理的默认值以确保现有数据兼容
5. 重启后端服务使更改生效

**关键代码位置**：`backend/main.py` 第202-216行 (数据库升级逻辑)

## 实施计划

### 阶段1：前端初始化优化（预计时间：10分钟）
1. 添加isInitializing状态变量
2. 修改onMounted逻辑，延迟渲染直到验证完成
3. 测试未登录和已登录两种场景

### 阶段2：前端数据加载增强（预计时间：15分钟）
1. 为loadDatasets等函数添加错误处理
2. 更新模板中的空值保护
3. 添加加载状态UI
4. 测试各菜单项切换

### 阶段3：后端数据库修复（预计时间：5分钟）
1. 验证数据库表结构升级代码是否正确
2. 删除旧数据库文件强制重新初始化（如果需要）
3. 重启后端服务
4. 测试数据集上传功能

### 阶段4：集成测试与验证（预计时间：10分钟）
1. 清除浏览器缓存和localStorage
2. 完整测试登录流程
3. 测试所有菜单项切换
4. 测试数据集上传功能
5. 验证无控制台错误

## 验收标准

### 功能验收
- [ ] 访问http://localhost:3000/时无闪烁，直接显示正确页面
- [ ] 登录后可正常切换到所有菜单页面（仪表板、模型管理、数据集管理、评估规则、评估任务、用户管理）
- [ ] 可成功上传CSV/Excel/JSON格式数据集
- [ ] 上传的数据集正确显示在列表中

### 性能验收
- [ ] 页面加载时间 < 2秒
- [ ] 菜单切换响应时间 < 500ms
- [ ] 无明显的UI闪烁或卡顿

### 错误处理验收
- [ ] token失效时自动跳转到登录页
- [ ] API请求失败时显示友好错误信息
- [ ] 控制台无未捕获的JavaScript错误

## 风险评估与缓解

### 高风险项
1. **数据库迁移可能导致数据丢失**
   - 缓解措施：使用ALTER TABLE而非DROP/CREATE，保留原有数据
   
2. **前端状态管理复杂度高**
   - 缓解措施：逐步修改，每步测试，避免大规模重构

### 中风险项
1. **浏览器兼容性问题**
   - 缓解措施：使用标准Web API，避免实验性特性

## 回退方案
如果修改导致更严重的问题：
1. 使用Git回滚到上一个稳定版本
2. 从备份恢复数据库文件
3. 清除浏览器localStorage重新测试
